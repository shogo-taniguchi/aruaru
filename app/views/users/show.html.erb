<h1>users#show</h1>
<p>名前 : <%= @user.name %></p>
<p>メールアドレス : <%= @user.email %></p>
<p>プロフィール : <%= @user.profile %></p>



<% if current_user.id == @user.id %>
  <%= link_to "編集する", edit_user_registration_path %>
<% end %>

<h2>ユーザーの投稿一覧</h2>
クレイマー
<% @user.claimers.each do |t| %>
  <%= t.body_c %>
<% end %>
</br>
バイトあるある
<% @user.posts.each do |t| %>
  <%= t.user.name %>
  <%= t.body_p %>
<% end %>

<!-- 以下はいいね機能を実装している場合-->
<h2>ユーザーがいいねした投稿一覧</h2>
<% @user.post_liked_posts.each do |t| %>
  <%= t.user.name %>
  <%= t.body_p %>
<% end %>
<!-- ここまで-->

<%= link_to "Tweet一覧に戻る", posts_path %>